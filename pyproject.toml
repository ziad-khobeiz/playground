[project]
name = "playground"
version = "0.1.0"
description = "Playground for experiments & projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "instructor>=1.13.0",
    "playwright>=1.57.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]
